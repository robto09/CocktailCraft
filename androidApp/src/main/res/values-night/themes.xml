<?xml version="1.0" encoding="utf-8"?>
<!-- Dark counterpart of values/themes.xml — see the note there. -->
<resources>
    <style name="Theme.CocktailBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/cocktail_primary_dark</item>
        <item name="colorPrimaryDark">@color/cocktail_primary_dark</item>
        <item name="colorAccent">@color/cocktail_secondary_dark</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:colorBackground">@color/background_dark</item>

        <!-- Status bar: app background under the (edge-to-edge) bar, light icons -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_dark</item>
    </style>
</resources>
